namespace TSLPatcher.Core.Compiler.NSS.AST;

/// <summary>
/// Control flow keywords in NSS.
/// </summary>
public enum ControlKeyword
{
    BREAK,
    CASE,
    DEFAULT,
    DO,
    ELSE,
    SWITCH,
    WHILE,
    FOR,
    IF,
    RETURN
}

